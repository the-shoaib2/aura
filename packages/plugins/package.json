{
	"name": "@aura/plugins",
	"version": "1.0.0",
	"description": "Plugins functionality of AURA",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"clean": "rimraf dist .turbo",
		"dev": "pnpm watch",
		"typecheck": "tsc --noEmit",
		"build": "tsc -p tsconfig.build.json",
		"format": "biome format --write .",
		"format:check": "biome ci .",
		"lint": "eslint . --quiet",
		"lint:fix": "eslint . --fix",
		"watch": "tsc -p tsconfig.build.json --watch",
		"test": "jest",
		"test:dev": "jest --watch",
		"manifest:generate": "ts-node scripts/generate-manifest.ts",
		"manifest:stubs": "ts-node scripts/create-plugin-stubs.ts"
	},
	"files": [
		"dist/**/*"
	],
	"dependencies": {
		"@aura/types": "workspace:*",
		"@aura/utils": "workspace:*",
		"@aura/constants": "workspace:*",
		"@slack/client": "^5.0.2",
		"googleapis": "^128.0.0",
		"@octokit/rest": "^20.0.0"
	},
	"devDependencies": {
		"@aura/typescript-config": "workspace:*",
		"@types/node": "*",
		"jest": "catalog:",
		"ts-jest": "catalog:",
		"typescript": "catalog:"
	},
	"license": "See LICENSE.md file in the root of the repository"
}
